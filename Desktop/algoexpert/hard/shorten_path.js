//Shorten Paths
//Stacks

//my understandings

//time complexity 
//O(n)

//space complexity 
//O(n)

function shortenPath(path) {
    const startWithSlash = path[0] === '/';
    const token = paths.split('/').filter(isImportantToken);
    const stack = [];
}
